# MySQL 8 target: --spring.profiles.active=mysql
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/demo?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC}
    username: ${DB_USERNAME:demo}
    password: ${DB_PASSWORD:demo}
